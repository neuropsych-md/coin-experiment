## Stimuli ##

Inkscape used to create svg files for the stimuli(MiSIT_layers) and the
legend(MiSIT_legend.svg).

2 versions of the MiSIT_layers file. One with the hatching at 45° (MiSIT_layers_45.svg) and
the other with the hatching at 30° and 60° (MiSIT_layers_30-60.svg)

svgtune is used to turn on and off the layers to create the different variations of the stimuli needed.

Each svg file has its own svgtune file:
MiSIT_layers_45.svg = MiSIT_layers_45.svgtune
MiSIT_layers_30-60.svg = MiSIT_layers_30-60.svgtune
MiSIT_legend.svg = MiSIT_legend.svgtune

To generate pngs with svgtune:
generate_pngs.sh filename(MiSIT_layers_45).svgtune

##Naming Scheme##

stimuli filenames:(array#)_(color)-(direction)_(relevant feature)-(cue location)
    ex: 1_green-SENW_farbe-left

array#: 1-16

color: green = g
       blue = b
       pink = p
       violet = v

direction: SENW & SWNE
S = bottom
N = top
W = left
E = right

The parts of a compass are used to describe the direction. "S"/South (bottom) and
"N"/North (top) are used to distinguish the rules. Should the subject pay attention
to the top of the circle or the bottom. 
